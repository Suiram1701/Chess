<UserControl x:Class="Chess.Figures.Farmer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Convert="clr-namespace:Chess.Figures.Convert"
             xmlns:local="clr-namespace:Chess.Figures"
             DataContext="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}"
             mc:Ignorable="d" 
             Height="100" Width="100" Canvas.Left="{Binding Position.X, Mode=OneWay}" Canvas.Top="{Binding Position.Y, Mode=OneWay}">
    <UserControl.Resources>
        <Convert:DataToImageConvert x:Key="DataToImage"/>
    </UserControl.Resources>
    <Grid Height="100" Width="100">
        <Image Source="{Binding Color, Converter={StaticResource DataToImage}, ConverterParameter=Farmer}" Height="100" Width="100" Stretch="Fill" Visibility="Visible" Panel.ZIndex="1"/>
    </Grid>
</UserControl>
